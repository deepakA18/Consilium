# deps
node_modules/
.pnp.*

# env / secrets
.env
.env.*
!.env.example

# builds
dist/
build/
out/
.next/
*.tsbuildinfo

# foundry
packages/contracts/out/
packages/contracts/cache/
packages/contracts/broadcast/
# Vendored Foundry deps (installed via forge, not submodules). Reinstall with:
#   cd packages/contracts && forge install smartcontractkit/chainlink-brownie-contracts OpenZeppelin/openzeppelin-contracts foundry-rs/forge-std --no-commit
packages/contracts/lib/

# event hub's last-round cache (regenerated each round)
packages/agents/.data/

# logs
*.log
npm-debug.log*
bun-debug.log*

# internal build spec — keep out of git (planning/source-of-truth doc, not shipped)
CONSOLIUM_BUILD.MD

# internal deploy notes — keep out of git
DEPLOY.md

# misc
.brand-preview/
next-env.d.ts
.DS_Store
coverage/
.vscode/
.idea/

# generated demo artifacts
packages/web/public/cards/*.png

