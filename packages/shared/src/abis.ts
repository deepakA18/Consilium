/**
 * Typed ABIs shared by agents & web.
 *
 * Populated in Step 2 once ConsiliumMarket.sol compiles — export the `as const` ABI here so
 * viem infers fully-typed reads/writes. Placeholder for now.
 */

export {};
